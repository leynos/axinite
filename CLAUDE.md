# Compatibility note

The canonical repository guidance now lives in `AGENTS.md`.

This file remains as a compatibility stub because some repository workflows,
automation prompts, and contributor references still look for a root
`CLAUDE.md` path.

When instructions appear to overlap, follow the guidance in `AGENTS.md` at the
repository root and any deeper-scoped `AGENTS.md` or `CLAUDE.md` files that
apply to the paths being changed.
