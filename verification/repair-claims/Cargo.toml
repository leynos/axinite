[package]
name = "repair-claims-verification"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[workspace]

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(kani)"] }
